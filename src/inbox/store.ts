// InboxStore removed. Manual inbox items are no longer tracked in plugin data.
// Files are added to the inbox by writing the #inbox tag to their frontmatter.
// Snooze state uses the `inbox-snooze` frontmatter field.
// All inbox logic now lives in InboxService (src/editor/InboxService.ts).
export {};
